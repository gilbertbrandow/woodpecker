services:
  backend:
    build:
      target: dev
    volumes:
      - ./backend/tests:/app/tests:ro
      - ./backend/pytest.ini:/app/pytest.ini:ro
    environment:
      SECRET_KEY: ci-test-secret
      LICHESS_CLIENT_ID: test
      LICHESS_REDIRECT_URI: http://localhost/callback
